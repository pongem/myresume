using the theme elegant



npm install -g jsonresume-theme-elegant
resume export resume.html --theme elegant
resume publish --theme elegant
resume login

npm install -g jsonresume-theme-simplistic
resume export resume.html --theme simplistic


note .. you will need to export to pdf first and then print (save) to pdf again to have an appropriate layout.